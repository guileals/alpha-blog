<h1 class="text-center mt-4">Lista de Artigos</h1>

<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false, :previous_label => '←', :next_label => '→' %>
</div>

<%= render 'article' %>

<div class="flickr_pagination mb-4">
    <%= will_paginate @articles, :container => false, :previous_label => '←', :next_label => '→' %>
</div>